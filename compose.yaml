services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: metrica
      MYSQL_USER: hm
      MYSQL_PASSWORD: hamid123
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    depends_on:
      - db
    environment:
      PMA_HOST: db
      PMA_USER: hm
      PMA_PASSWORD: hamid123
      PMA_ARBITRARY: 1
      PMA_SSL: "false"
      PMA_SSL_VERIFY: "false"
      PMA_PORT: 3306
    ports:
      - "8081:80"
    restart: unless-stopped

  app:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - db
    volumes:
      - ./:/app
    command: ["php-fpm"]
    restart: unless-stopped
    env_file:
      - .env.docker

  caddy:
    image: caddy:latest
    depends_on:
      - app
    ports:
      - "80:80"      # HTTP pour local
      - "443:443"     # HTTPS auto-signé pour local
    environment:
      SITE_HOST: ${SITE_HOST:-localhost}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./:/app
      - caddy_data:/data
      - caddy_config:/config
    restart: unless-stopped

volumes:
  db_data:
  caddy_data:
  caddy_config: